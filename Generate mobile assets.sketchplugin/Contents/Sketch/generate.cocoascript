@import "lib/document_parser.js"

var onRun = function(context) {
  var parsed = DocumentParser.parse(context.document)
  log(parsed)
}

var android = function(context) {
  log("TODO: generate assets for android.")
}

var ios = function(context) {
  log("TODO: generate assets for ios.")
}
